import styled from 'styled-components';

export const WorkDetailAto = () => {

  return(
    <ContainerStyle>
      <p>WorkDetailAto</p>
    </ContainerStyle>
  );
}

const ContainerStyle = styled.div`
  
`;
